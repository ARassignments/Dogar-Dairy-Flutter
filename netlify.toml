[build]
  command = "flutter build web --release"
  publish = "build/web"
  
  # Netlify redirect rule for Flutter web applications (handles client-side routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200